stages:
  - create-mr

create_merge_request:
  stage: create-mr
  image: alpine:latest
  rules:
    - if: '$CI_COMMIT_BRANCH =~ /^feature\//'
  script:
    - apk add --no-cache curl jq
    - sh scripts/create-mr.sh